name: "CodeQL Config"

queries:
  - uses: security-and-quality

query-filters:
  - exclude:
      id: js/incomplete-url-substring-sanitization
      # Suppress incomplete URL substring sanitization warnings
      # This is a legitimate use case where we're intercepting requests to our own API
      # The URL validation is intentional and safe in this context